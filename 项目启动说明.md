# 智能化健康管理系统 — 启动说明

> 一行命令启动：**双击 `start.bat`**（自动完成全部步骤）

---

## 快速启动（一键）

```bat
# 直接双击项目根目录的 start.bat，或命令行执行：
cd D:\code\health
start.bat
```

启动后访问：
| 地址 | 说明 |
|------|------|
| http://localhost:5173 | **前端页面** |
| http://localhost:8000/docs | **后端 API 文档（Swagger）** |
| http://localhost:8000/api/health | **健康检查接口** |

---

## 分步启动（手动方式）

### 1. 启动 MySQL

```bat
"D:\Java\MySQL\MySQL Server 8.0\bin\mysqld.exe" --defaults-file="D:\Java\MySQL\MySQL Server 8.0\my.ini"
```

验证是否启动成功：
```bat
"D:\Java\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -proot --protocol=TCP -P 3306 -e "SELECT 1;"
```
返回 `1` 即表示成功。

### 2. 启动后端（端口 8000）

```bat
cd D:\code\health\backend
pip install -r requirements.txt    # 首次需要安装依赖
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

验证：访问 http://localhost:8000/api/health ，应返回：
```json
{"status":"ok","app":"智能化健康管理系统","version":"1.0.0"}
```

### 3. 启动前端（端口 5173，新开一个终端）

```bat
cd D:\code\health\frontend
npm install       # 首次需要安装依赖（已完成）
npx vite --port 5173
```

验证：访问 http://localhost:5173 ，应看到登录/仪表盘页面。

---

## 环境要求

| 依赖 | 版本 | 说明 |
|------|------|------|
| Python | >= 3.11 | 已安装于 `C:\Users\lenovo\AppData\Local\Programs\Python\Python314` |
| MySQL | 8.0 | 已安装于 `D:\Java\MySQL\MySQL Server 8.0` |
| Node.js | >= 18 | 前端构建所需 |

---

## 配置文件

### 数据库配置（`backend/.env`）

```env
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/health?charset=utf8mb4
```

数据库信息：
| 项目 | 值 |
|------|-----|
| 地址 | localhost:3306 |
| 数据库名 | health |
| 用户名 | root |
| 密码 | root |

### API Key 配置（同样在 `backend/.env`）

| Key | 用途 | 不配置的影响 |
|-----|------|-------------|
| `DEEPSEEK_API_KEY` | OCR 体检报告识别 + AI 健康分析/咨询 | OCR 与 AI 分析功能均不可用 |

不配 Key 也不影响核心功能（指标录入、规则引擎评估、健康看板）。

---

## 项目结构速览

```
D:\code\health\
├── start.bat             ★ 一键启动脚本（双击即可）
├── 项目启动说明.md         ★ 本文件
├── 项目开发状态.md         开发进度记录
├── 产品介绍.md             产品文档
│
├── backend/               后端（Python FastAPI）
│   ├── main.py            入口文件
│   ├── config.py          配置加载
│   ├── .env               环境变量（数据库/API Key）
│   ├── requirements.txt   Python 依赖
│   ├── app/
│   │   ├── database.py    数据库连接
│   │   ├── models/        数据模型（user, health_indicator, chat）
│   │   ├── schemas/       请求/响应模型
│   │   ├── routers/       API 路由（user, health, chat, ocr, classify）
│   │   └── services/      业务逻辑（规则引擎、AI、OCR）
│   └── tests/             测试
│
└── frontend/              前端（React + Vite + Ant Design）
    ├── package.json       Node 依赖
    ├── vite.config.js     Vite 配置（含 API 代理）
    └── src/
        ├── main.jsx       入口
        ├── pages/         页面（Dashboard, Assessment, Chat, OCR, Health, Profile）
        ├── components/    公共组件
        └── api/index.js   API 请求封装
```

---

## API 接口一览（共 24 个）

### 用户档案
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/users/default` | 获取/创建默认用户 |
| GET | `/api/users/{id}` | 获取用户 |
| PUT | `/api/users/{id}` | 更新用户 |
| POST | `/api/users` | 创建用户 |

### 健康指标
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health/indicators` | 指标列表（支持筛选） |
| POST | `/api/health/indicators` | 添加指标（自动规则评估） |
| POST | `/api/health/indicators/batch` | 批量添加 |
| GET | `/api/health/indicators/{id}` | 指标详情 |
| PUT | `/api/health/indicators/{id}` | 更新指标（重新评估） |
| DELETE | `/api/health/indicators/{id}` | 删除指标 |
| GET | `/api/health/categories` | 指标分类列表 |
| GET | `/api/health/risk-summary` | 风险评估摘要 |
| GET | `/api/health/suggestions` | 四维度健康建议 |
| POST | `/api/health/ai-analyze` | AI 综合分析 |
| GET | `/api/health/ai-analyses` | 已保存的 AI 分析记录 |

### AI 健康咨询
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/chat/sessions` | 会话列表 |
| POST | `/api/chat/sessions` | 创建会话 |
| DELETE | `/api/chat/sessions/{id}` | 删除会话 |
| GET | `/api/chat/sessions/{id}/messages` | 消息历史 |
| POST | `/api/chat/send` | 发送消息（AI 回复） |

### OCR / 医学影像
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ocr/recognize` | 上传体检报告识别 |
| POST | `/api/classify/skin` | 纯本地皮肤状况图片初筛 |
| GET | `/api/classify/classes` | 分类类别列表 |
| GET | `/api/classify/status` | 模型状态 |

---

## 常见问题

**Q: 启动报错 `No module named 'xxx'`？**
```bat
cd D:\code\health\backend
pip install -r requirements.txt
```

**Q: MySQL 无法启动？**
- 确认路径：MySQL 安装在 `D:\Java\MySQL\MySQL Server 8.0`
- 检查端口 3306 是否被占用：`netstat -ano | findstr :3306`
- 或直接双击 `start.bat` 自动处理

**Q: 前端页面白屏/无法访问？**
- 确认后端已启动（访问 http://localhost:8000/api/health）
- 前端启动需要几秒钟，稍等刷新即可

**Q: 数据库 `health` 不存在？**
```bat
"D:\Java\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -proot -e "CREATE DATABASE health CHARACTER SET utf8mb4;"
```

---

*生成日期：2026-07-18*
