# 智能化健康管理系统 — 项目开发状态

> 最后更新：2026-07-11

---

## 一、总体进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| 需求分析 | ✅ 完成 | 产品文档已定稿 |
| 技术选型 | ✅ 完成 | Python + FastAPI + MySQL + React |
| 后端基础框架 | ✅ 完成 | 项目骨架、数据库、配置 |
| 用户档案模块 | ✅ 完成 | CRUD + BMI 自动计算 |
| 健康指标模块 | ✅ 完成 | CRUD + 规则引擎评估 |
| 规则引擎 | ✅ 完成 | 6 类指标覆盖 |
| 风险评估 | ✅ 完成 | 按分类汇总 + 整体风险 |
| 健康建议 | ✅ 完成 | 四维度自动生成 |
| AI 咨询接口 | ✅ 完成 | 需 DeepSeek API Key |
| OCR 识别接口 | ✅ 完成 | 需 Kimi API Key |
| 数据库 | ✅ 完成 | 已切换为 MySQL |
| 启动脚本 | ✅ 完成 | start.bat 一键启动 |
| 前端开发 | ✅ 完成 | React 18 + Vite + Ant Design + 动画 |
| 仪表盘页面 | ✅ 完成 | 数据概览、风险评分、图表、AI 分析 |
| 健康指标页面 | ✅ 完成 | 列表/图表双视图、CRUD、ECharts |
| AI 咨询页面 | ✅ 完成 | 会话管理、实时对话、图片上传 |
| OCR 识别页面 | ✅ 完成 | 拖拽上传、识别结果展示 |
| 风险评估页面 | ✅ 完成 | 健康评分、分类风险详情 |
| 用户档案页面 | ✅ 完成 | 信息编辑、BMI 计算 |
| 医学影像分类 | ✅ 完成 | ONNX Runtime + MobileNetV2，10.35 MB 权重 |
| 影像分类页面 | ✅ 完成 | 图片上传、低置信度提示、模型状态、Top-K 结果 |
| 模型训练资源 | ✅ 完成 | SCIN 快速子集下载器与训练验收说明 |
| Docker 部署 | 📅 规划中 | 容器化 |
| 用户权限系统 | 📅 规划中 | 多用户支持 |

---

## 二、后端模块状态

### 2.1 数据库模型（全部完成）

| 模型 | 表名 | 字段数 | 说明 |
|------|------|--------|------|
| User | `users` | 10 | 用户档案 + BMI 属性 |
| HealthIndicator | `health_indicators` | 12 | 健康指标 + 评估结果 |
| ChatSession | `chat_sessions` | 6 | AI 对话会话 |
| ChatMessage | `chat_messages` | 7 | 对话消息 |

### 2.2 API 接口（全部完成）

#### 用户档案 `/api/users`
- `GET /default` — 获取/创建默认用户
- `GET /{id}` — 获取用户
- `PUT /{id}` — 更新用户
- `POST /` — 创建用户

#### 健康指标 `/api/health`
- `GET /indicators` — 列表（支持分类/状态筛选）
- `POST /indicators` — 创建（自动规则评估）
- `POST /indicators/batch` — 批量创建
- `GET /indicators/{id}` — 详情
- `PUT /indicators/{id}` — 更新（自动重新评估）
- `DELETE /indicators/{id}` — 删除
- `GET /categories` — 分类列表
- `GET /risk-summary` — 风险评估摘要
- `GET /suggestions` — 四维度建议
- `POST /ai-analyze` — AI 综合分析（有 API Key 时）

#### AI 咨询 `/api/chat`
- `GET /sessions` — 会话列表
- `POST /sessions` — 创建会话
- `GET /sessions/{id}` — 会话详情
- `DELETE /sessions/{id}` — 删除会话（级联消息）
- `GET /sessions/{id}/messages` — 消息历史
- `POST /send` — 发送消息 + AI 回复

#### OCR 识别 `/api/ocr`
- `POST /recognize` — 体检报告识别 + 自动保存

#### 医学影像分类 `/api/classify`
- `POST /skin` — 皮肤病变分类（JSON 请求体）
- `GET /classes` — 支持类别列表
- `GET /status` — 模型/依赖状态

### 2.3 服务层（全部完成）

| 服务 | 文件 | 说明 |
|------|------|------|
| 规则引擎 | `rule_engine.py` | 6 类指标正常范围/风险判定/建议生成 |
| AI 服务 | `ai_service.py` | DeepSeek API 封装，含降级方案 |
| OCR 服务 | `ocr_service.py` | Kimi 视觉大模型封装 |
| 健康服务 | `health_service.py` | 业务编排（规则+AI）+ 四维度建议 |

### 2.4 规则引擎覆盖指标

| 分类 | 指标数 | 示例 |
|------|--------|------|
| 血糖 blood_sugar | 4 | 空腹血糖、餐后2h血糖、糖化血红蛋白、随机血糖 |
| 血压 blood_pressure | 2 | 收缩压、舒张压 |
| 血脂 blood_fat | 4 | 总胆固醇、甘油三酯、HDL、LDL |
| 肝功能 liver | 3 | ALT、AST、总胆红素 |
| 肾功能 kidney | 3 | 肌酐、尿素氮、尿酸 |
| 血常规 blood_routine | 4 | 白细胞、红细胞、血红蛋白、血小板 |
| **合计** | **20** | |

---

## 三、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | FastAPI | 0.138 |
| Python | CPython | 3.14 |
| 数据库 | MySQL | 8.0.41 |
| ORM | SQLAlchemy | 2.0 |
| 数据验证 | Pydantic | 2.13 |
| AI 模型接口 | DeepSeek V4 Flash | — |
| OCR 模型接口 | Kimi moonshot-v1 | — |
| 前端 | React + Vite + Ant Design | — |

---

## 四、部署信息

### 开发环境

| 项目 | 值 |
|------|-----|
| 后端地址 | `http://localhost:8000` |
| API 文档 | `http://localhost:8000/docs` |
| 数据库 | `mysql://root:root@localhost:3306/health` |
| 数据目录 | `E:\d\MySQL\MySQL Server 8.0\Data` |

### 启动方式

```bash
# 启动 MySQL
mysqld --defaults-file="E:/d/MySQL/MySQL Server 8.0/my.ini"

# 启动后端
cd backend && uvicorn main:app --reload --port 8000
```

或双击 `start.bat` 一键启动。

---

## 五、待办任务

### 优先级高
- [ ] 配置 DeepSeek API Key 启用 AI 功能
- [ ] 配置 Kimi API Key 启用 OCR 功能

### 优先级中
- [x] 医学影像分类模块（ONNX Runtime + MobileNetV2）
- [x] SCIN 常见皮肤状况训练子集下载器
- [x] 前端健康数据图表（ECharts）
- [ ] 前端 Chat 界面（流式回复）

### 优先级低
- [ ] 多用户权限系统
- [ ] Docker 容器化
- [ ] HTTPS + Nginx 反向代理
- [ ] npm install 自动安装前端依赖

---

## 六、已知问题

1. **MySQL 开机自启** — MySQL 未注册为 Windows 服务，重启后需手动启动
2. **无 AI 降级体验** — 未配 API Key 时 AI 功能会返回提示信息，核心规则引擎不受影响
3. **端口冲突** — 如果 8000 端口被占用，启动会失败，需修改端口
